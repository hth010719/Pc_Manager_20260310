plugins {
    id 'java'
    id 'application'
}

group = 'com.pcmanager'
version = '0.1.0'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

repositories {
    mavenCentral()
}

application {
    mainClass = 'com.pcmanager.ServerMain'
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}
